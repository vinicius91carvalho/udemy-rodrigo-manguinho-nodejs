version: "3"
services:
  mongo:
    container_name: mongo-container
    image: mongo:4
    restart: always
    volumes:
      - ./data:/data/db
    ports:
      - "27017:27017"
  api:
    container_name: api-container
    image: node:14
    working_dir: /usr/clean-node-api
    image: clean-node-api
    restart: always
    command: bash -c "npm run debug && npm install --only=prod"
    volumes:
      - ./dist:/usr/clean-node-api/dist
      - ./package.json:/usr/clean-node-api/package.json
    ports:
      - "5050:5050"
      - "9222:9222"
    links:
      - mongo
    
  
